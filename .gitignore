# secrets — never commit
.env

# gitclaw runtime state
.gitagent/
memory/.latest-frame.jpg
memory/journal/
memory/runtime/

# skill_learner bookkeeping is stripped before commit; ignore stray caches
**/.DS_Store

# internal handoff notes — not for the public repo
Context.md
